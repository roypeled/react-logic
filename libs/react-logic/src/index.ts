/**
 * @module @react-logic/react-logic
 *
 * Umbrella entry — re-exports the three required packages so consumers can
 * install and import from a single name. Optional packages
 * (`@react-logic/utils`, `@react-logic/angular-adapter`) are not bundled
 * here; install them directly when needed.
 */
export * from '@react-logic/core';
export * from '@react-logic/state';
export * from '@react-logic/di';
